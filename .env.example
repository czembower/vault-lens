# LLM Provider configuration
# Options: anthropic (default), openai
LLM_PROVIDER=anthropic

# Anthropic Claude API configuration (required if LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=sk_

# OpenAI API configuration (required if LLM_PROVIDER=openai)
OPENAI_API_KEY=sk-
OPENAI_MODEL=gpt-4-turbo

# Vault Audit MCP server (stdio transport)
# Path to the vault-audit-mcp binary
VAULT_AUDIT_MCP_COMMAND=./vault-audit-mcp

# Loki configuration for vault-audit-mcp
# Set this to your Loki instance URL (defaults to http://localhost:3100)
LOKI_URL=http://localhost:3100

# Vault MCP server (HTTP URL on port 8080)
VAULT_MCP_URL=http://localhost:8080

# Backend API server port
API_PORT=3001

# Frontend dev server port
VITE_PORT=5173
